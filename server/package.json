{
  "name": "mota-scholarship-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend for the AI-Enabled Scholarship and Fellowship Management System (Ministry of Tribal Affairs)",
  "main": "src/index.js",
  "scripts": {
    "dev": "nodemon src/index.js",
    "start": "node src/index.js",
    "seed": "node src/seed.js",
    "migrate": "node -e \"import('./src/db/pool.js').then(async m=>{await m.migrate();await m.closePool();})\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.x"
  }
}
